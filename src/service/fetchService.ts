import React from 'react';
import { spaceXSiteTemplate } from '../common/types/types';

const endpoint: string = 'https://api.spacex.land/graphql/';

async function fetchData() {}

export default fetchData;
